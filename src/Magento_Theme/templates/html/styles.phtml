<?php
/**
 * Module for asynchronous CSS loading which speeds up first page render.
 * Just add path to your desired stylesheet to the array below.
 */
?>
<script>
[
].forEach( function( url ) {
    loadCSS( url );
} );
</script>
